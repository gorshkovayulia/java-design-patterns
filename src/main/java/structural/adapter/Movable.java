package structural.adapter;

// Movable interface is supposed to return the speed of some luxury cars in miles per hour
public interface Movable {
    // returns speed in MPH
    double getSpeed();
}
